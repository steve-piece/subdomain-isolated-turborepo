# -----------------------------------------------------------------------------
# App Identity
# -----------------------------------------------------------------------------
# Marketing domain (public website)
# Search for `marketingdomain.com` in your codebase with match case and whole word enabled, and replace all with your actual marketing domain
NEXT_PUBLIC_MARKETING_DOMAIN='marketingdomain.com'

# Protected app domain (multi-tenant subdomains)
# Format: <subdomain>.protecteddomain.com
# Search for `protecteddomain.com` in your codebase with match case and whole word enabled, and replace all with your actual protected domain
NEXT_PUBLIC_APP_DOMAIN='protecteddomain.com'

# Display name shown across all apps
# Search for `Your App` in your codebase with match case and whole word enabled, and replace all with your actual app name
# Use NEXT_PUBLIC_APP_NAME to make it available in client components, or NEXT_PUBLIC_APP_NAME for server-only
NEXT_PUBLIC_APP_NAME='Your App'

# -----------------------------------------------------------------------------
# Supabase Configuration
# Retrieved from: https://supabase.com/dashboard/project/_/settings/api
# -----------------------------------------------------------------------------
# Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=

# Supabase publishable key (anon key)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# Supabase secret key (service_role key)
SUPABASE_SECRET_KEY=

# -----------------------------------------------------------------------------
# Email Configuration (Resend)
# API key retrieved from: https://resend.com/api-keys
# -----------------------------------------------------------------------------
RESEND_API_KEY=""
NEXT_PUBLIC_SENDER_EMAIL='info@emaildomain.com'
NEXT_PUBLIC_EMAIL_DOMAIN='emaildomain.com'
NEXT_PUBLIC_SUPPORT_EMAIL='support@emaildomain.com'
RESEND_ENABLE_INVITATION_EMAILS='true'
RESEND_ENABLE_WELCOME_EMAILS='true'
RESEND_VERIFY_EMAILS='true'

# -----------------------------------------------------------------------------
# Stripe Configuration (Protected App Only)
# API keys retrieved from: https://dashboard.stripe.com/apikeys
# Webhook secret retrieved from: https://dashboard.stripe.com/webhooks
# Price IDs retrieved from: https://dashboard.stripe.com/products (click product > pricing)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_FREE_MONTHLY_PRICE_ID=
STRIPE_FREE_YEARLY_PRICE_ID=
STRIPE_PRO_MONTHLY_PRICE_ID=
STRIPE_PRO_YEARLY_PRICE_ID=
STRIPE_BUSINESS_MONTHLY_PRICE_ID=
STRIPE_BUSINESS_YEARLY_PRICE_ID=
STRIPE_ENTERPRISE_MONTHLY_PRICE_ID=
STRIPE_ENTERPRISE_YEARLY_PRICE_ID=

# -----------------------------------------------------------------------------
# OPTIONAL: Monitoring & Analytics
# -----------------------------------------------------------------------------
# Sentry - Retrieved from: https://sentry.io/settings/projects/ (select project > Client Keys for DSN)
# Auth token retrieved from: https://sentry.io/settings/account/api/auth-tokens/
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Checkly - Retrieved from: https://app.checklyhq.com/settings/account/api-keys
CHECKLY_API_KEY=

# Environment tracking
NEXT_PUBLIC_APP_ENV=''