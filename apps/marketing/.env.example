# =============================================================================
# MARKETING APP - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# This app handles public marketing pages and organization signup

# -----------------------------------------------------------------------------
# REQUIRED: Domain Configuration
# -----------------------------------------------------------------------------
# Marketing domain (e.g., "yourapp.com" or "localhost:3002" for local dev)
NEXT_PUBLIC_MARKETING_DOMAIN=localhost:3002

# Protected app domain where users will be redirected after signup
# (e.g., "app.yourapp.com" or "localhost:3003" for local dev)
NEXT_PUBLIC_APP_DOMAIN=localhost:3003

# -----------------------------------------------------------------------------
# REQUIRED: App Identity
# -----------------------------------------------------------------------------
# Display name shown in UI, emails, and metadata
APP_NAME=Your App

# -----------------------------------------------------------------------------
# REQUIRED: Supabase Configuration
# -----------------------------------------------------------------------------
# Your Supabase project URL (e.g., "https://xxxxx.supabase.co")
NEXT_PUBLIC_SUPABASE_URL=

# Supabase publishable/anon key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL: Monitoring & Analytics
# -----------------------------------------------------------------------------
# Sentry error tracking (leave empty to disable)
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Environment identifier for Sentry (defaults to NODE_ENV if not set)
# NEXT_PUBLIC_APP_ENV=production

# Git commit SHA for release tracking (auto-populated by Vercel)
# NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA=
# VERCEL_GIT_COMMIT_SHA=

# -----------------------------------------------------------------------------
# LOCAL DEVELOPMENT TIPS
# -----------------------------------------------------------------------------
# - Marketing app runs at: http://localhost:3002
# - Protected app runs at: http://localhost:3003
# - Tenant subdomains: http://<subdomain>.localhost:3003
#
# Example .env.local for local development:
# NEXT_PUBLIC_MARKETING_DOMAIN=localhost:3002
# NEXT_PUBLIC_APP_DOMAIN=localhost:3003
# APP_NAME=My App
# NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=eyJhbGc...
