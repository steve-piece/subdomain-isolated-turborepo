# =============================================================================
# TURBOREPO ROOT - Environment Variables
# =============================================================================
# This is the root .env.example for the entire monorepo.
# Each app (marketing & protected) has its own .env.example with app-specific variables.
#
# For local development, copy this file to .env.local at the root level.
# Then also create .env.local files in:
# - apps/marketing/.env.local
# - apps/protected/.env.local
#
# See individual app .env.example files for detailed variable descriptions.

# -----------------------------------------------------------------------------
# REQUIRED: Domain Configuration
# -----------------------------------------------------------------------------
# Marketing domain (public website)
# Search for `marketingdomain.com` in your codebase with match case and whole word enabled, and replace all with your actual marketing domain
NEXT_PUBLIC_MARKETING_DOMAIN=marketingdomain.com

# Protected app domain (multi-tenant subdomains)
# Format: <subdomain>.protecteddomain.com
# Search for `protecteddomain.com` in your codebase with match case and whole word enabled, and replace all with your actual protected domain
NEXT_PUBLIC_APP_DOMAIN=protecteddomain.com

# -----------------------------------------------------------------------------
# REQUIRED: App Identity
# -----------------------------------------------------------------------------
# Display name shown across all apps
# Search for `Your App` in your codebase with match case and whole word enabled, and replace all with your actual app name
APP_NAME='Your App'

# -----------------------------------------------------------------------------
# REQUIRED: Supabase - Client Configuration
# -----------------------------------------------------------------------------
# Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=

# Supabase publishable key (anon key)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# -----------------------------------------------------------------------------
# REQUIRED: Supabase - Server Configuration (Protected App Only)
# -----------------------------------------------------------------------------
# Supabase project URL (server-side)
SUPABASE_URL=

# Supabase secret key (service role key - NEVER expose in browser)
SUPABASE_SECRET_KEY=

# -----------------------------------------------------------------------------
# REQUIRED: Stripe Configuration (Protected App Only)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Stripe Price IDs (get from Stripe Dashboard)
STRIPE_FREE_MONTHLY_PRICE_ID=
STRIPE_FREE_YEARLY_PRICE_ID=
STRIPE_PRO_MONTHLY_PRICE_ID=
STRIPE_PRO_YEARLY_PRICE_ID=
STRIPE_ENTERPRISE_MONTHLY_PRICE_ID=
STRIPE_ENTERPRISE_YEARLY_PRICE_ID=

# -----------------------------------------------------------------------------
# OPTIONAL: Email Configuration (Resend)
# -----------------------------------------------------------------------------
# RESEND_API_KEY=
# SENDER_EMAIL=noreply@emaildomain.com
# SUPPORT_EMAIL=support@emaildomain.com
# EMAIL_DOMAIN=emaildomain.com
# RESEND_ENABLE_INVITATION_EMAILS=true
# RESEND_ENABLE_WELCOME_EMAILS=true
# RESEND_VERIFY_EMAILS=true

# -----------------------------------------------------------------------------
# OPTIONAL: Database Configuration
# -----------------------------------------------------------------------------
# Usually auto-populated by Supabase/Vercel
# POSTGRES_URL=
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=
# POSTGRES_HOST=
# POSTGRES_USER=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=
# SUPABASE_JWT_SECRET=
# SUPABASE_SSL_CERT=

# -----------------------------------------------------------------------------
# OPTIONAL: Monitoring & Analytics
# -----------------------------------------------------------------------------
# Sentry
# SENTRY_DSN=
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=

# Checkly
# CHECKLY_API_KEY=

# Environment tracking
# NEXT_PUBLIC_APP_ENV=production
# VERCEL_GIT_COMMIT_SHA=
# NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA=

# -----------------------------------------------------------------------------
# OPTIONAL: Customization
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_DEFAULT_LOGO_URL=
# NEXT_PUBLIC_SUPPORT_EMAIL=support@emaildomain.com

# -----------------------------------------------------------------------------
# LOCAL DEVELOPMENT SETUP
# -----------------------------------------------------------------------------
# 1. Copy this file: cp .env.example .env.local
# 2. Copy app configs:
#    - cp apps/marketing/.env.example apps/marketing/.env.local
#    - cp apps/protected/.env.example apps/protected/.env.local
# 3. Fill in required values in each .env.local file
# 4. Start development:
#    - pnpm dev (starts all apps)
#    - OR individually:
#      - cd apps/marketing && pnpm dev (port 3002)
#      - cd apps/protected && pnpm dev (port 3003)
#
# URLs:
# - Marketing: http://localhost:3002
# - Protected: http://localhost:3003
# - Tenants: http://<subdomain>.localhost:3003
